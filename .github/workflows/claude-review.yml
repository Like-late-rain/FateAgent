name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main
      - develop

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    name: Claude AI Review

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: |
            apps/**/*.{ts,tsx,js,jsx,py}
            packages/**/*.ts

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Anthropic SDK
        run: npm install @anthropic-ai/sdk

      - name: Run Claude Review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: node .github/scripts/claude-review.js

      - name: Post Review Comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const result = JSON.parse(fs.readFileSync('review-result.json', 'utf-8'));

            let body = `## 🤖 Claude AI 代码审核\n\n`;

            // 决策图标
            const decisionIcon = {
              'approve': '✅ 通过',
              'request_changes': '❌ 需要修改',
              'comment': '💬 评论'
            };
            body += `### 审核结果: ${decisionIcon[result.decision] || result.decision}\n\n`;

            if (result.summary) {
              body += `**总结**: ${result.summary}\n\n`;
            }

            // 红线违规（最重要）
            if (result.redline_violations && result.redline_violations.length > 0) {
              body += `### 🚨 红线违规\n\n`;
              body += `> ⚠️ 以下问题**必须修复**才能合并\n\n`;
              for (const v of result.redline_violations) {
                body += `- **${v.rule}** - \`${v.file}\`${v.line ? `:${v.line}` : ''}\n`;
                body += `  > ${v.description}\n\n`;
              }
            }

            // 类型一致性
            if (result.type_consistency && !result.type_consistency.passed) {
              body += `### ⚠️ 类型一致性问题\n\n`;
              for (const issue of result.type_consistency.issues || []) {
                body += `- ${issue}\n`;
              }
              body += `\n`;
            }

            // 其他问题
            if (result.issues && result.issues.length > 0) {
              body += `### 发现的问题\n\n`;
              for (const issue of result.issues) {
                const icon = issue.severity === 'critical' ? '🚨' :
                             issue.severity === 'warning' ? '⚠️' : '💡';
                body += `${icon} **${issue.severity.toUpperCase()}** - \`${issue.file}\`\n`;
                body += `> ${issue.description}\n`;
                if (issue.suggestion) {
                  body += `> 💡 ${issue.suggestion}\n`;
                }
                body += `\n`;
              }
            }

            // 详细反馈
            if (result.comment) {
              body += `### 详细反馈\n\n${result.comment}\n`;
            }

            // 发布 Review
            // 注意: GitHub Actions 不允许 approve 自己的 PR，所以 approve 时使用 COMMENT
            const event = result.decision === 'request_changes' ? 'REQUEST_CHANGES' : 'COMMENT';
            await github.rest.pulls.createReview({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              body: body,
              event: event
            });
